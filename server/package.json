{
  "name": "insurancexdate-mcp",
  "version": "1.5.0",
  "description": "InsuranceXDate MCP/REST adapter for WC, benefits, SERFF, account research, and default-off agency actions, with guarded native search and sanitized account status.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "pretest": "npm run build",
    "test": "node --test test/*.test.mjs && node scripts/smoke-test-tools-list.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^5.9.3"
  }
}
